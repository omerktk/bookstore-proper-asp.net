@model IEnumerable<myshop.feed>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_adminlayout1.cshtml";
}

<h2>Feedbacks</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Questions
        </th>
        <th>
            Answers
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.feedq)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.feeda)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.feedid }) |
                @Html.ActionLink("Details", "Details", new { id = item.feedid }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.feedid })
            </td>
        </tr>
    }

</table>
